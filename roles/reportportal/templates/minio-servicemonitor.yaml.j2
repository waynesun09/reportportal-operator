apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: minio
  name: minio-metrics
  namespace: {{ meta.namespace }}
spec:
  endpoints:
    - port: '9000'
      path: /minio/prometheus/metrics
  selector:
    matchLabels:
      app: minio
