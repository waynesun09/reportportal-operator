---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgresql
  name: postgresql
  namespace: {{ meta.namespace }}
spec:
  ports:
  - name: tcp-postgresql
    port: {{ rp_db_port }}
    protocol: TCP
    targetPort: tcp-postgresql
  selector:
    app: postgresql
    role: master
  type: ClusterIP
