apiVersion: v1
kind: Service
metadata:
  labels:
    app: minio
  name: minio
  namespace: {{ meta.namespace }}
spec:
  ports:
  - name: service
    port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: minio
  type: ClusterIP
